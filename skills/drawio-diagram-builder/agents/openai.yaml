interface:
  display_name: "Research Draw.io Diagram Builder"
  short_description: "Build editable research draw.io figures with screenshot self-review"
  default_prompt: "Use $drawio-diagram-builder to create or revise the requested editable diagram. Reuse existing source and assets, export affected requested formats, and inspect the rendered result for semantic and visual defects."
